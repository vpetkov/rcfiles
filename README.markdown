### Configuration files

**vim**  
`cp -rf vim ~/.vim  
echo "source ~/.vim/vimrc.vim" > ~/.vimrc`  
**git**  
`cat gitconfig > ~/.gitconfig`  
**bash**  
`cp -rf bash ~/.bash
cat bashrc >> ~/.bashrc`  
**mutt**  
`cp -rf mutt ~/.mutt  
echo "source ~/.mutt/muttrc.mutt" > ~/.muttrc`  
