### Configuration files

**vim**  
`cp -rf vim ~/.vim  
echo "source ~/.vim/vimrc.vim" > ~/.vimrc`  
**git**  
`cat gitconfig > ~/.gitconfig`  
**bash**  
`cp -rf bash ~/.bash
cat bash_profile >> ~/.bash_profile`
**mutt**  
`cp -rf mutt ~/.mutt  
cat muttrc > ~/.muttrc
